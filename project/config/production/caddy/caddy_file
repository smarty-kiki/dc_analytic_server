https://dc.smarty.so {
    tls kiki@smarty.so
    root /var/www/dc_analytic_server/public
    fastcgi / /var/run/php-fpm.sock php
    rewrite / {
        regexp .*
        to /index.php{uri}
    }
    git {
        repo https://github.com/smarty-kiki/dc_analytic_server.git
        branch master
        path /var/www/dc_analytic_server
        interval 180
        then ln -fs /var/www/dc_analytic_server/project/config/production/supervisor/queue_worker.conf /etc/supervisor/conf.d/queue_worker.conf
        then ln -fs /var/www/dc_analytic_server/project/config/production/supervisor/caddy.conf /etc/supervisor/conf.d/caddy.conf
        then supervisorctl update
    }
}
